import { StatusReservationPipe } from './status-reservation.pipe';

describe('StatusReservationPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusReservationPipe();
    expect(pipe).toBeTruthy();
  });
});
