import { TypeFacilityPipe } from './type-facility.pipe';

describe('TypeFacilityPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeFacilityPipe();
    expect(pipe).toBeTruthy();
  });
});
